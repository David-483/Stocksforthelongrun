<script>
  

  function makedata(category,firstJahr, lastYear, ) {
    if(firstJahr == null){
      firstJahr = 2013;
    }
    if(lastYear == null){
      lastYear = new Date().getFullYear() -1;
    }    
    var DATA = new Array();
    var JAHR= firstJahr;
    var BASE;
    for (let i = firstJahr; i < lastYear; i++) {
      BASE = "year_" + JAHR;
      console.log(BASE);
      console.log(myObj[category][BASE].FY);
      DATA.push(myObj[category][BASE].FY);
      JAHR= JAHR+1;
    }
    return DATA;
  }
  
  function makedescription(firstJahr, lastYear, ) {
    if(firstJahr == null){
      firstJahr = 2013;
    }
    if(lastYear == null){
      lastYear = new Date().getFullYear() -1;
    }    
    var DATA = new Array();
    var JAHR= firstJahr;
    for (let i = firstJahr; i < lastYear; i++) {
      console.log(JAHR);
      DATA.push(JAHR);
      JAHR= JAHR+1;
    }
    return DATA;
  }

  var revenue = makedata("revenue",2013,2021);
  var description = makedescription(2013,2021);

  var netincome = makedata("net_income",2013,2021);
  var description = makedescription(2013,2021);

  var ebit = makedata("EBIT",2013,2021);
  var description = makedescription(2013,2021);

  var optionsArea = {  chart: {
    height: 328,
    type: 'bar',
    zoom: {
      enabled: false
    },
    dropShadow: {
      enabled: true,
      top: 3,
      left: 2,
      blur: 4,
      opacity: 1,
    }
  },
  stroke: {
    curve: 'smooth',
    width: 2
  },
   series: [{
      name: "Umsatz",
      data: revenue
    },
    {
      name: "EBIT",
      data: ebit
    },
    {
      name: "Gewinn",
      data: netincome
    }
  ],
    xaxis: {
      categories: description,
    },
    tooltip: {
      followCursor: true
    },
    markers: {
    size: 5,
    strokeWidth: 0,
    hover: {
      size: 7
    }
  },
    fill: {
      opacity: 1,
    },
  }
</script>